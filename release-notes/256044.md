- [Fixes from v1.23.6 to v1.23.8](https://dev.azure.com/schwarzit-wiking/schwarzit.stackit-dev-exp/_git/stackit-git-forgejo/pullrequest/256044)
  - Fixed a `%!s(BADINDEX)` rendering error in the issue/PR timeline for `closed_at`, `reopened_at`, `commit_ref_at`, and `ref_*_from` events, caused by a custom locale override wrapping the timestamp placeholder in an extra argument that the template didn't pass.
  - Restored several pieces of functionality that had silently fallen out of sync between custom template overrides and the underlying Forgejo templates: the navbar-hiding check for mandatory 2FA enrollment, the admin Security configuration section and router log indicator, the push-mirror branch filter field and issue-reindex button in repository settings, the archived-repository guard on the "New Issue" button, and the username-rename-disabled check and avatar settings anchor on the profile page.
  - Set a default SQLite database path in the PR environment Terraform config map so the install page's SQLite path field no longer renders empty.
