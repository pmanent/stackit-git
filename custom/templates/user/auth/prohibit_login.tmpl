{{template "base/head" .}}
<div role="main" aria-label="{{.Title}}" class="page-content user activate">
	<div class="ui middle very relaxed page grid">
		<div class="column">
			<form class="ui form tw-max-w-2xl tw-m-auto">
				<h2 class="ui top attached header">
					{{ctx.Locale.Tr "auth.prohibit_login"}}
				</h2>
				<div class="ui attached segment">
					<p class="tw-m-0">{{ctx.Locale.Tr "auth.prohibit_login_desc_1"}}</p>
					<br>
					<p class="tw-m-0">{{ctx.Locale.Tr "auth.prohibit_login_desc_2"}}</p>
					<p class="tw-m-0">{{ctx.Locale.Tr "auth.prohibit_login_desc_3"}}</p>
					<p class="tw-m-0">{{ctx.Locale.Tr "auth.prohibit_login_desc_4"}}</p>
					<br>
					<p class="tw-m-0">{{ctx.Locale.Tr "auth.prohibit_login_desc_5"}}</p>
					<p class="tw-m-0">{{ctx.Locale.Tr "auth.prohibit_login_desc_6"}}</p>
				</div>
			</form>
		</div>
	</div>
</div>
{{template "base/footer" .}}
